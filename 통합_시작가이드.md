# 한미르 통합최종본 A to Z 시작 가이드

이 폴더는 위치/UWB 최신본을 기준으로 음성·AI와 YOLO 카메라 담당 코드를 통합한 실행본입니다.

## 1. 최초 한 번 설치

PowerShell에서 이 폴더로 이동한 뒤 실행합니다.

```powershell
.\install_windows.bat
.\install_ai_windows.bat
```

- Python 3.12를 우선 사용합니다. 이 PC에는 통합 작업 중 Python 3.12가 설치되었습니다.
- 두 번째 명령은 실제 `best.pt` YOLO 분석용입니다.
- 설치가 끝나면 `.venv`와 `frontend\node_modules`가 생깁니다.

## 2. 백엔드 AI 설정

```powershell
Copy-Item .\backend\.env.example .\backend\.env
notepad .\backend\.env
```

- `OPENAI_API_KEY=`는 선택입니다.
- 키가 없을 때도 위치, 지도, 카메라, 위험도, 고정 음성 응답은 동작합니다.
- 키가 있으면 실제 음성 STT와 세이피 GPT 응답이 활성화됩니다.
- 키와 Wi-Fi 비밀번호는 GitHub에 올리지 마세요.

## 3. 안전모 AV 장치 최초 설정과 업로드

```powershell
.\.venv\Scripts\python.exe .\configure_av_device.py
[System.IO.Ports.SerialPort]::GetPortNames()
.\.venv\Scripts\python.exe .\av_tool.py upload --port COM번호
```

`av_tool.py`는 한글 Windows 경로에서 ESP32-S3 컴파일러가 실패하는 문제를 자동으로 처리합니다. 빌드만 하려면 `build`, 로그를 보려면 `monitor --port COM번호`를 사용합니다.

통합 AV 펌웨어 기능:

- OV5640 JPEG 촬영 및 3초마다 서버 전송
- INMP441 2초 WAV 녹음 및 서버 STT 업로드
- MAX98357A I2S 경고음
- 버튼 1회: 녹음, 3회: 비상 전송과 현장 경고음
- WebSocket 실시간 경고/녹음 명령
- 실제 부품 초기화 상태 heartbeat 보고

## 4. UWB 장치

기존에 업로드한 앵커 1~4와 안전모 TAG 펌웨어는 그대로 사용합니다. Wi-Fi 또는 서버 PC 주소가 바뀐 경우에만 기존 `configure_wifi_tag.py`, `uwb_tool.py` 절차로 TAG를 다시 업로드합니다.

## 5. 전체 실제 하드웨어 실행

```powershell
.\run_integrated_hardware.bat
```

그다음 다음 장치에 전원을 넣습니다.

1. UWB 앵커 1~4
2. 안전모 UWB TAG
3. 안전모 AV 장치
4. 웹 브라우저에서 `http://localhost:5173`

정상이라면 서버만 PC에서 실행하고 모든 ESP32는 보조배터리/전원만 연결해도 Wi-Fi로 데이터를 보냅니다.

## 6. 하드웨어가 없을 때 카메라 테스트

서버와 웹을 켠 상태에서 별도 PowerShell을 열고 실행합니다.

```powershell
.\run_camera_mock.bat
```

`sample_data\test_images`의 3개 프레임이 서버로 전송되고, 카메라 관제·작업자 PPE·위험도·이벤트 사진이 갱신됩니다.

## 7. 웹 메뉴 확인 순서

1. 통합 대시보드: 전체 장치/위험도/최근 이벤트
2. 실시간 지도: UWB 작업자 위치와 위험구역
3. 카메라 관제: 최근 YOLO 주석 영상과 안전모·조끼·장갑·화재 상태
4. 음성·AI: 작업자별 명령, 의도, 응답, TTS 확인
5. 지도 설계: 작업장 크기, 앵커, 장애물, 위험구역을 저장 후 적용
6. 위치 기록 재생: 이동 이력 확인
7. 작업자 관리: 이름과 특이사항 수정
8. 하드웨어 진단: 카메라·마이크·스피커·버튼·UWB 수신 확인

## 현재 하드웨어 제한

- ESP32 스피커는 지금 경고음/톤을 실제 출력합니다.
- 세이피의 자연어 TTS MP3는 웹에서 재생됩니다. MP3 음성을 안전모 스피커에서 직접 재생하려면 ESP32용 MP3 디코더/스트리밍 단계가 추가로 필요합니다.
- 4개 UWB 앵커는 2D 위치용입니다. 정확한 Z축 3D 위치는 높이가 다른 앵커를 추가한 뒤 확장합니다.