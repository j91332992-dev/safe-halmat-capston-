# 한미르 스마트 안전모 프로젝트 인계 및 필수 세팅 가이드

작성일: 2026-08-09  
목적: 다른 작업자가 다른 컴퓨터에서 현재 작업을 그대로 이어받아 최종 기능 검사를 진행하기 위한 인계 문서

## 1. 프로젝트 및 Git 정보

프로젝트 폴더:

```text
C:\Users\조성준\OneDrive - pukyong.ac.kr\바탕 화면\한미르최종본
```

GitHub 저장소:

```text
https://github.com/j91332992-dev/safe-halmat-capston-.git
```

현재 로컬 브랜치와 커밋:

```text
브랜치: agent/complete-safety-voice-ui
커밋: 765a8cd 안전모 음성·화재 오탐·통합 UI 보완
```

해당 커밋은 로컬에 생성됐지만 원격 저장소에 아직 push되지 않았을 수 있다. 작업을 시작하기 전에 확인한다.

```powershell
cd "C:\Users\조성준\OneDrive - pukyong.ac.kr\바탕 화면\한미르최종본"
git status -sb
git log -1 --oneline
git branch --show-current
```

원격 저장소에 브랜치가 없다면 다음 명령을 실행한다.

```powershell
git push -u origin agent/complete-safety-voice-ui
```

## 2. 보안상 GitHub에 올리면 안 되는 파일

다음 파일은 실제 비밀번호, API 키, 토큰이 들어갈 수 있으므로 커밋하지 않는다.

```text
backend\.env
firmware\helmet_av_device\include\device_secrets.h
```

필수 원칙:

- 실제 Wi-Fi 비밀번호를 GitHub에 올리지 않는다.
- `OPENAI_API_KEY`, 통화 토큰 등 실제 값을 커밋하지 않는다.
- `.env.example`에는 예시 또는 빈 값만 둔다.
- 백엔드와 ESP32에 설정된 통화 토큰은 서로 일치해야 한다.

## 3. 현재 하드웨어 구성

안전모 AV 장치:

- ESP32-S3 N16R8
- OV5640 카메라: 보드 중앙 FPC 소켓 사용
- INMP441 디지털 마이크
- MAX98357A I2S 앰프와 스피커
- UWB 태그
- 배터리 → TP4056 → 전원 스위치 → 승압 모듈 → 각 장치

재배선 후 실제 오디오 핀:

```text
INMP441
SCK  → GPIO1
WS   → GPIO2
SD   → GPIO42

MAX98357A
BCLK → GPIO41
LRC  → GPIO40
DIN  → GPIO39
```

카메라와 오디오 GPIO 충돌을 피하도록 재배선이 완료된 상태다.

UWB 구성:

- 앵커 4개
- 안전모 태그 1개
- 태그가 Wi-Fi를 통해 백엔드로 거리 데이터를 전송
- 최종 사용 시 앵커와 태그는 컴퓨터 USB가 아닌 전원만 공급
- 서버 PC와 ESP32 장치는 서로 통신 가능한 같은 공유기 네트워크 사용

## 4. 지금까지 실제 확인한 기능

- UWB 앵커 4개 거리 수신
- 2D 위치 계산 및 웹 지도 표시
- 앵커별 수신 상태 표시
- 안전모 AV 및 UWB 장치 온라인/오프라인 표시
- 카메라 촬영, 서버 전송 및 웹 표시
- INMP441 마이크 입력
- MAX98357A 스피커 출력
- 호출어 `투투스` 인식
- 호출 후 “네, 말씀하세요” 출력
- 후속 음성 명령 STT 처리
- 유사 문장에 대한 관리자 통화 요청 처리
- 브라우저와 안전모 사이 양방향 음성통화
- 통화 종료 후 음성비서 모드 복귀
- `도와줘` SOS 생성, 지도 표시, 신고 확인 및 종료
- 화재 음성 신고
- YOLO 화재 감지 후 관리자 확인 팝업
- 화재 구간 지정, 지도 표시, 오판단 취소 및 화재 없음 처리
- 작업자 위치·위험도·보호구 음성 안내
- 작업장 크기, 장애물, 위험구역, 앵커 위치 편집
- 작업자 이름과 특이사항 관리

## 5. 최근 음성 기능 수정 사항

### 5.1 호출 직후 마이크 재개 지연 감소

기존에는 “네, 말씀하세요”가 끝난 직후 말하면 첫 음절이 잘릴 수 있었다.

```text
스피커 재생 후 마이크 대기 시간: 약 700ms → 약 80ms
```

목표는 안내 직후 기다리지 않고 말해도 첫 음절부터 인식하는 것이다.

### 5.2 5초 무응답 자동 취소

“네, 말씀하세요” 이후 약 5초 동안 발화가 없으면:

- 음성 세션 자동 종료
- 짧은 하강형 종료 효과음 출력
- 평상시 호출 대기 상태로 복귀

### 5.3 STT 결과 처리

- 사람이 말하면 VAD로 발화를 감지해 자동 녹음한다.
- 명령을 알아들으면 짧은 성공 효과음을 내고 해당 기능을 실행한다.
- 말을 했지만 이해하지 못하면 “다시 한 번 말씀해 주세요”를 출력한다.
- 빈 STT 결과도 방치하지 않고 재시도 흐름으로 처리한다.
- 문장이 달라도 의미가 같으면 같은 기능으로 처리한다.

관리자 통화 계열 예시:

```text
관리자 연결해줘
관리자에게 전화해줘
관리자에게 연락해줘
팀장에게 연결해줘
팀장하고 통화할래
관제실 연결해줘
현장소장에게 전화해줘
```

## 6. 가장 먼저 해야 할 작업

최신 AV 펌웨어는 빌드까지 성공했으나 실제 ESP32-S3에는 아직 업로드하지 않았다. 따라서 인계 후 첫 작업은 최신 AV 펌웨어 업로드다.

### 6.1 ESP32-S3 USB 연결

1. 안전모 배터리 전원 스위치를 끈다.
2. ESP32-S3의 USB 데이터 포트를 컴퓨터에 연결한다.
3. 충전 전용이 아닌 데이터 통신 가능한 케이블을 사용한다.
4. COM 포트를 확인한다.

```powershell
[System.IO.Ports.SerialPort]::GetPortNames()
```

기존 COM4, COM5 외에 새로 나타난 포트가 ESP32-S3일 가능성이 높다. COM 번호는 연결할 때마다 바뀔 수 있으므로 이전 번호를 단정하지 않는다.

### 6.2 최신 AV 펌웨어 업로드

```powershell
cd "C:\Users\조성준\OneDrive - pukyong.ac.kr\바탕 화면\한미르최종본"
.\.venv\Scripts\python.exe .\av_tool.py upload --port COM번호
```

예: COM13인 경우

```powershell
.\.venv\Scripts\python.exe .\av_tool.py upload --port COM13
```

성공 기준:

```text
Hash of data verified.
Hard resetting via RTS pin...
[SUCCESS]
```

업로드 연결 오류가 날 때만 다음 순서를 사용한다.

1. BOOT 버튼을 누른 상태 유지
2. RST 버튼을 한 번 눌렀다 놓기
3. BOOT 버튼 놓기
4. 업로드 다시 실행

BOOT 버튼은 평상시 음성비서 호출에 사용하지 않는다.

## 7. 새 컴퓨터 필수 프로그램과 설치

권장 환경:

```text
Windows 10/11
PowerShell 7 또는 명령 프롬프트
Python 3.12 또는 3.13 권장
Node.js LTS
Git
GitHub CLI(선택)
```

Python 3.14에서는 고정된 SciPy 버전 설치 오류가 발생한 적이 있으므로 Python 3.12를 권장한다.

```powershell
python --version
node --version
npm --version
git --version
```

최초 설치:

```powershell
cd "C:\Users\조성준\OneDrive - pukyong.ac.kr\바탕 화면\한미르최종본"
.\install_windows.bat
```

PowerShell에서는 현재 폴더의 배치 파일 앞에 `\.\`를 붙여야 한다.

기존 컴퓨터의 `.venv`는 다른 컴퓨터에서 그대로 사용하지 말고 다시 생성한다.

설치 후 확인:

```powershell
.\.venv\Scripts\python.exe --version
```

프런트엔드 패키지가 없다면 프런트엔드 폴더에서 `npm install`을 실행한다.

## 8. 백엔드 환경변수 설정

새 컴퓨터에는 `backend\.env`가 자동으로 내려오지 않는다.

```powershell
Copy-Item ".\backend\.env.example" ".\backend\.env"
```

그 후 필요한 AI/STT/TTS API 키와 통화 토큰을 안전하게 입력한다. 기존 컴퓨터의 실제 키는 GitHub나 공개 메신저로 전달하지 않는다.

## 9. 서버 PC IPv4와 Wi-Fi 설정

서버 PC의 현재 IPv4 확인:

```powershell
ipconfig
```

주소 의미:

```text
127.0.0.1 또는 localhost: 현재 PC 자체에서만 접속
192.168.x.x: ESP32가 접속할 실제 서버 PC 주소
0.0.0.0: 서버 바인딩 주소이며 브라우저 입력용이 아님
```

ESP32에는 `127.0.0.1`이 아니라 서버 PC의 실제 `192.168.x.x` 주소가 들어가야 한다.

ESP32-S3는 2.4GHz Wi-Fi를 사용한다. 기존 사용 SSID는 `Gaon303_2.5G`였으나 새 환경에서는 실제 SSID를 입력한다.

AV 장치 설정:

```powershell
.\.venv\Scripts\python.exe .\configure_av_device.py
```

입력 항목:

- 2.4GHz Wi-Fi SSID
- Wi-Fi 비밀번호
- 서버 PC IPv4

설정 파일:

```text
firmware\helmet_av_device\include\device_secrets.h
```

설정 변경 후 반드시 AV 펌웨어를 다시 업로드한다.

UWB 태그 설정:

```powershell
.\.venv\Scripts\python.exe .\configure_wifi_tag.py
```

설정 후 UWB 태그 펌웨어도 다시 업로드한다. 서버 PC의 IP가 바뀌면 AV와 UWB 설정을 모두 확인한다. 가능하면 공유기에서 서버 PC DHCP 예약 또는 고정 IP를 사용한다.

## 10. Windows 방화벽 및 서버 포트

필요 포트:

```text
백엔드 TCP 8000
프런트엔드 TCP 5173
```

처음 실행할 때 Windows 보안 경고가 뜨면 현재 사용하는 사설 네트워크 접근을 허용한다.

8000번 포트 확인:

```powershell
Get-NetTCPConnection -LocalPort 8000 -State Listen
```

`WinError 10048`은 백엔드를 두 번 실행했거나 다른 프로그램이 8000번 포트를 사용 중이라는 뜻이다. 한미르 백엔드 프로세스가 확실할 때만 기존 프로세스를 종료한다.

## 11. 서버와 웹 실행

백엔드만 실행:

```powershell
cd "C:\Users\조성준\OneDrive - pukyong.ac.kr\바탕 화면\한미르최종본"
.\run_backend.bat
```

통합 실행:

```powershell
.\run_all.bat
```

웹 접속:

```text
http://localhost:5173
```

백엔드 확인 주소:

```text
http://127.0.0.1:8000
```

`run_all.bat` 실행 시 화재 시뮬레이터가 자동으로 실행되지 않도록 수정된 상태다.

`Uvicorn running on http://0.0.0.0:8000`이 표시된 백엔드 창은 기능 검사 중 닫지 않는다. 백엔드를 두 번 실행하지 않는다.

## 12. 브라우저 마이크 권한

관리자 통화에는 브라우저 마이크 권한이 필요하다.

1. Chrome에서 `http://localhost:5173` 접속
2. 주소창 왼쪽 사이트 정보 아이콘 클릭
3. 마이크를 `허용`으로 변경
4. 새로고침 또는 `Ctrl + F5`
5. Windows 소리 설정에서 올바른 입력 장치 확인

관리자 PC에서는 마이크 보안 정책 문제를 줄이기 위해 `localhost:5173`을 사용한다.

## 13. 안전모 전원 및 충전 주의사항

정상 사용 시 ESP32 USB 연결은 필요 없다. USB는 펌웨어 업로드와 로그 확인에만 사용한다. 기능 검사는 안전모 배터리 전원만으로 해야 한다.

전원 켠 후 확인 순서:

1. 배터리 충전 상태 확인
2. 안전모 전원 스위치 켜기
3. ESP32-S3와 UWB LED 확인
4. 10~15초 기다리기
5. 웹 장치 관리에서 온라인 확인

LED가 켜져 있다는 사실만으로 Wi-Fi와 서버 연결 완료를 의미하지는 않는다.

정상 연결 조건:

- ESP32 및 UWB 전원 정상
- 2.4GHz Wi-Fi 연결
- 서버 PC IP 정확
- 백엔드 실행 중
- Windows 방화벽 허용
- 백엔드와 장치의 토큰 일치

반복 재부팅이나 LED 꺼짐이 있으면 배터리 부족, 승압 출력 불안정, 순간 전류 부족, USB와 배터리 동시 전원, 접촉 불량을 확인한다.

TP4056은 LED 색만으로 단정하지 말고 기판의 `CHRG`, `STDBY/FULL` 표시를 기준으로 충전 상태를 판단한다. 충전 LED가 전혀 없으면 케이블, 충전기, 입력 단자, 배터리 결선 또는 모듈 고장을 확인한다.

## 14. 카메라 주의사항

카메라는 전원을 끈 상태에서만 FPC 케이블을 탈착한다.

정상 로그:

```text
[CAMERA] 초기화 성공
[CAMERA] 캡처 전송 -> HTTP 200
```

다음 오류는 케이블 방향, 미삽입 또는 잠금 불량 가능성이 높다.

```text
Camera probe failed with error 0x105
ESP_ERR_NOT_FOUND
```

밝은 조명이나 반사가 화재로 오인될 수 있어 현재 화재 감지 조건은 confidence 0.60 이상 및 3개 연속 프레임으로 보완돼 있다. 실제 화재 확정 전 관리자 확인 절차를 유지한다.

## 15. 펌웨어 업로드 후 필수 음성 검사

### 검사 준비

1. 백엔드와 프런트엔드 실행
2. USB를 분리하고 안전모 배터리 전원 켜기
3. 약 15초 기다리기
4. 장치 관리에서 `helmet-001-av` 온라인 확인
5. 브라우저 마이크 권한 확인

### A. 즉시 후속 발화

1. `투투스`라고 말한다.
2. “네, 말씀하세요”가 끝나는 즉시 기다리지 않고 `관리자 연결해줘`라고 말한다.
3. 첫 음절이 잘리지 않는지, 성공음이 나는지, 통화 요청과 연결이 되는지 확인한다.

### B. 5초 무응답 종료

1. `투투스`라고 말한다.
2. 안내 후 아무 말도 하지 않는다.
3. 약 5초 후 종료 효과음이 나고 호출 대기 상태로 복귀하는지 확인한다.

### C. 이해하지 못한 발화

의미 없는 말 또는 미지원 명령을 말한 뒤 “다시 한 번 말씀해 주세요”가 나오는지 확인한다.

### D. 유사 문장 범용성

```text
팀장에게 연결해줘
팀장에게 전화해줘
팀장에게 연락해줘
관리자하고 통화할래
관제실 연결해줘
현장소장에게 연락해줘
```

모두 관리자 통화 기능으로 이어지는지 확인한다.

### E. 다른 음성 기능

```text
현재 위치 알려줘
현재 위험도 알려줘
보호구 상태 알려줘
도와줘
화재가 발생했어
비상 상황이야
```

위치·위험도·보호구는 안내, `도와줘`는 SOS, 화재 표현은 관리자 화재 확인 절차로 이어져야 한다.

### F. 통화 종료 복귀

통화를 종료한 뒤 다시 `투투스`를 호출해 음성비서가 정상 작동하는지 반드시 확인한다.

## 16. 문제 발생 시 시리얼 로그

ESP32-S3를 USB 데이터 포트로 연결한 후 현재 COM 번호를 확인하고 실행한다.

```powershell
cd "C:\Users\조성준\OneDrive - pukyong.ac.kr\바탕 화면\한미르최종본"
.\.venv\Scripts\python.exe .\av_tool.py monitor --port COM번호
```

정상 부팅 기준:

```text
[BOOT] 안전모 AV 최종 통합 펌웨어 시작
[SPEAKER] MAX98357A I2S 초기화 성공
[AUDIO] INMP441 준비 완료
[CAMERA] 초기화 성공
[WIFI] 연결 성공
[SERVER] 장치 등록 성공
[WS] 서버 명령 채널 연결 성공
```

다음 오류는 서버가 꺼져 있거나 해당 주소에서 백엔드가 실행되지 않은 경우이며, 그 자체로 하드웨어 고장을 뜻하지 않는다.

```text
connection refused
not connected
```

## 17. 과거 이벤트 정리

웹에 과거 SOS나 화재 팝업이 남아 있으면 새 감지가 아니라 데이터베이스에 남은 미종료 이벤트일 수 있다. 기능 검사 전에 다음 버튼으로 정리한다.

```text
신고 확인
상황 종료
화재 없음
오판단으로 취소
```

PC 날짜·시간 자동 설정과 서울 시간대도 확인한다.

## 18. 매번 권장하는 시작 순서

1. PC를 장치와 통신 가능한 공유기 네트워크에 연결
2. `ipconfig`로 서버 IPv4 확인
3. IP가 변경됐다면 AV/UWB 설정 후 펌웨어 재업로드
4. 안전모와 앵커 전원은 아직 끄기
5. 백엔드와 프런트엔드 실행
6. `http://localhost:5173` 접속
7. Chrome 마이크 권한 허용
8. 안전모 전원 켜기
9. UWB 검사 시 앵커 4개 전원 켜기
10. 약 15초 기다리기
11. AV 및 UWB 장치 온라인 확인
12. 대시보드에서 A1~A4 수신 확인
13. 남아 있는 SOS·화재 이벤트 종료
14. `투투스` 음성 기능 검사
15. 문제가 있을 때만 USB 연결 후 시리얼 로그 확인

## 19. 절대 하면 안 되는 것

- 실제 키, 토큰, Wi-Fi 비밀번호를 GitHub에 올리지 않는다.
- 카메라 전원이 켜진 상태에서 FPC 케이블을 탈착하지 않는다.
- 백엔드를 두 번 실행하지 않는다.
- `0.0.0.0:8000`을 브라우저 주소로 입력하지 않는다.
- 펌웨어 업로드 중 USB 케이블을 분리하지 않는다.
- COM 번호를 이전 번호로 단정하지 않는다.
- 사용자 변경사항이 있는 상태에서 `git reset --hard`를 사용하지 않는다.
- 기능 검사 중 화재 시뮬레이터를 자동 실행하지 않는다.
- YOLO 한 프레임 감지만으로 실제 화재를 확정하지 않는다.

## 20. 현재 검증 결과

현재 커밋 기준:

```text
백엔드 테스트: 43 passed
프런트엔드 빌드: 성공
ESP32-S3 AV 펌웨어 빌드: 성공
RAM 사용량: 약 15.6%
Flash 사용량: 약 32.6%
```

## 21. 최종 완료 기준

- 최신 AV 펌웨어 실장치 업로드
- 배터리 전원만으로 AV 장치 온라인
- 배터리 전원만으로 UWB 위치 전송
- 카메라 연속 전송
- `투투스` 호출 정상
- 안내 직후 발화 첫 음절 정상 인식
- 유효 명령 성공음
- 실패 명령 재요청 음성
- 5초 무응답 종료음
- SOS 생성·확인·종료
- 화재 감지·관리자 확인·구역 지정·취소
- 관리자 통화 및 양방향 음성 확인
- 통화 종료 후 음성비서 자동 복귀
- 서버와 PC 재시작 후 장치 자동 재연결
- 전체 테스트와 빌드 성공
- 민감정보 제외 후 GitHub 최종 반영

## 22. 인계 후 즉시 수행할 순서 요약

1. Git 브랜치와 커밋 확인
2. 새 컴퓨터에서 의존성 재설치
3. `.env`, Wi-Fi, 서버 IP, 토큰 설정
4. 최신 AV 펌웨어를 ESP32-S3에 업로드
5. USB를 분리하고 배터리 전원으로 부팅
6. 장치 관리 온라인 확인
7. 호출 직후 발화, 5초 무응답, STT 실패 재요청 검사
8. 유사 문장과 전체 음성 기능 검사
9. 통화 종료 후 음성비서 복귀 검사
10. 이상이 있으면 시리얼 로그를 근거로 수정
11. 모든 검사 완료 후 민감정보를 제외하고 GitHub에 최종 반영
